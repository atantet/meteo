# Dépendances pip pour Streamlit Community Cloud (App 2 Opérationnelle).
#
# Streamlit Cloud installe via pip et n'utilise PAS environment.yml
# (depuis ~2024). Pour éviter qu'il essaie quand même conda et plante,
# environment.yml est renommé en environment-dev.yml. Ce fichier liste
# le sous-ensemble minimal pour faire tourner
# apps/operationnelle/streamlit_app.py via pip.
#
# CI et dev local utilisent environment-dev.yml (conda) — pas ce fichier.
# Garder les deux en cohérence (mettre à jour les deux si on ajoute une
# dep utilisée par l'app Streamlit).
streamlit
pandas
numpy
pyyaml
python-dotenv
requests
# Importé transitivement par meteo_socle.indices (calculs solaires
# Penman-Monteith). Présence requise même si l'app op ne fait que la
# prévision : l'ETP est calculée côté socle.
pvlib
# Backend Parquet pour pandas. Utilisé par le rapport climato et,
# défensivement, si l'app op lit un cache à terme.
pyarrow
# Figures App 2 Op (courbes avec overlay normale + bilan culture).
matplotlib
